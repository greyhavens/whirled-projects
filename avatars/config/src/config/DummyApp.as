package config {

import flash.display.Sprite;

public class DummyApp extends Sprite
{
    // Just reference the relevant classes here so that Flex Builder will compile
    // everything and give compile errors.
    ConfigData;
    ConfigBody;
    ConfigPanel;
}

}
