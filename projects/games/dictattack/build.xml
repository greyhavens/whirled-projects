<!-- build configuration -->
<project name="dictattack" default="compile" basedir=".">

  <!-- define some basic project parameters -->
  <property name="app.name" value="DictionaryAttack"/>
  <property name="app.package-dir" value="dictattack"/>

  <!-- include the standard build script -->
  <import file="../../../etc/project-include.xml"/>

</project>
